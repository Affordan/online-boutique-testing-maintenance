apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-coupon-f001
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: one
  duration: 10m
  selector:
    namespaces:
      - online-boutique
    labelSelectors:
      app: coupon-service
