apiVersion: v1
kind: Service
metadata:
  name: user-log-service
  namespace: online-boutique
  labels:
    app: user-log-service
    monitoring: enabled
spec:
  type: ClusterIP
  selector:
    app: user-log-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
