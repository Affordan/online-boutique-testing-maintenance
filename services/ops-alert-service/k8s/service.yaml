apiVersion: v1
kind: Service
metadata:
  name: ops-alert-service
  namespace: online-boutique
  labels:
    app: ops-alert-service
    monitoring: enabled
spec:
  type: ClusterIP
  selector:
    app: ops-alert-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
