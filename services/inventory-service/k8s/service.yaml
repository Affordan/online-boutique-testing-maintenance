apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: online-boutique
  labels:
    app: inventory-service
    monitoring: enabled
spec:
  type: ClusterIP
  selector:
    app: inventory-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
